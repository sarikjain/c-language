//Write a C program to calculate area of a rectangle using hard coded inputs.
#include<stdio.h>

int main()
{
    int length = 3;
    int breadth = 6;

    printf("The area of this rectangle is %d", length*breadth);
    return 0;
}